import { HeroLinkColorDirective } from './hero-link-color.directive';

describe('HeroLinkColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroLinkColorDirective();
    expect(directive).toBeTruthy();
  });
});
